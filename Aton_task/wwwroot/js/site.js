function changeVision(loginButtons, logoutButton) {
    var button = document.getElementById(loginButtons);
    button.style.display = "none";
    var logout = document.getElementById(logoutButton);
    logout.style.display = "block";
}
